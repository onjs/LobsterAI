{
  "version": "v1.1",
  "vector_store": {
    "provider": "qdrant",
    "config": {
      "host": "qdrant",
      "port": 6333,
      "collection_name": "mem0"
    }
  },
  "llm": {
    "provider": "openai",
    "config": {
      "model": "MiniMax-M2.7",
      "temperature": 0.2,
      "api_key": "${OPENAI_API_KEY}",
      "openai_base_url": "${OPENAI_BASE_URL}"
    }
  },
  "embedder": {
    "provider": "openai",
    "config": {
      "model": "${MEM0_EMBEDDER_MODEL}",
      "api_key": "${OPENAI_API_KEY}",
      "openai_base_url": "${OPENAI_BASE_URL}"
    }
  }
}
